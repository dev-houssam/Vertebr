[package]
name = "system_module"
version = "1.0.0"
edition = "2021"
authors = ["Houssam"]
license = "MIT"

[lib]
name = "system_module"
crate-type = ["cdylib"]

[dependencies]
serde      = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
